import * as GraphQL from "..";
export declare const name: (prefix: string, name: string) => string;
export declare const document: (prefix: string, document: GraphQL.DocumentNode) => GraphQL.DocumentNode;
